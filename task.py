
def solve(claw_pos, boxes, box_in_claw):
    """
    claw_pos: int - index of the stack the arm is currently above
    boxes: List[int] - number of boxes in each stack
    box_in_claw: bool - True if the claw is holding a box
    Returns: str - one of "LEFT", "RIGHT", "PICK", "PLACE" or "None" only 
    """
    # Your code implementation here decides what the next move is.
    return None